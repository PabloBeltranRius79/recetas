doctype html
html
	head
		meta(charset="utf-8")
		title Recetas
	body
		h1 Recetas del Restaurante Pablo Beltrán Salus
		p #{mensaje}
		form(id="nuevaRecetaForm" action="/nuevaReceta" method="post")
			p
				label(for="nombreReceta") Nombre de la receta:
				input(type="text" id="nombreReceta" name="nombre")
			p
				label(for="descripcionReceta") Descripcion:
				input(type="text" id="descripcionReceta" name="descripcion")
			p
				label(for="fechaReceta") Fecha:
				input(type="date" id="fechaReceta" name="fecha_publicacion")
			p
				input(type="submit" value="publicar")
		